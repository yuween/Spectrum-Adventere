// Collision event with obj_wall
if (speed > 0) {
    speed = 0; // Stop movement
    // Optionally reset direction or perform other logic
    direction = -1; // Example: Reset direction to an invalid state
}
